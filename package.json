{
  "name": "@liquid-labs/catalyst-app-core",
  "version": "0.0.2",
  "description": "Core Catalyst app libraries.",
  "main": "dist/index.js",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "babel --source-maps inline src -d dist",
    "start": "babel --watch --source-maps inline src -d dist",
    "pretest": "babel --source-maps inline src -d dist",
    "test": "jest --coverage dist",
    "prepare": "rm -rf dist && babel src -d dist && find dist -name '*.test.js' | xargs rm"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Liquid-Labs/catalyst-app-core.git"
  },
  "keywords": [
    "react"
  ],
  "author": "Zane Rockenbaugh <zane@liquid-labs.com>",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Liquid-Labs/catalyst-app-core/issues"
  },
  "homepage": "https://github.com/Liquid-Labs/catalyst-app-core#readme",
  "devDependencies": {
    "@babel/cli": "^7.0.0",
    "@babel/core": "^7.0.1",
    "@babel/preset-env": "^7.0.0",
    "babel-plugin-transform-export-extensions": "^6.22.0",
    "jest": "^23.6.0"
  },
  "dependencies": {
    "@liquid-labs/redux-followup": "0.0.2",
    "@liquid-labs/regex-repo": "0.0.5",
    "lodash.camelcase": "^4.3.0",
    "lodash.omit": "^4.5.0",
    "lodash.reduce": "^4.6.0",
    "moment-timezone": "^0.5.21",
    "normalizr": "^3.2.4",
    "redux": "^4.0.0",
    "redux-thunk": "^2.3.0"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
